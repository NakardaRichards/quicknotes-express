{
  "version": 2,
  "builds": [
    { "src": "api/notes.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/notes.js" },
    { "src": "/(.*)", "dest": "public/$1" }
  ]
}